resources:
  - deployment.yml
  - service.yml

namespace: flask-gke-app

configMapGenerator:
  - name: flask-config
    literals:
      - FLASK_ENV=production
      - FLASK_RUN_HOST=0.0.0.0
      - FLASK_RUN_PORT=5000